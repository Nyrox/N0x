// IR (maybe not neccesary? dunno)
.main:
	// Constants resolved
	float x = 8.25
	float y = x * x
	call_extern print y
	return

// Compiles into (human readable form)
.main:
	push 8.25
	push mul $0x00 $0x00

	// call_extern name [bytes to copy, memory locations to copy from]
	call_extern print 0x20 $0x20


